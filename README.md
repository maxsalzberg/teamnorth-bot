# TeamNorth Discord Bot

Discord бот, созданный с использованием discord.js v14.

## 🚀 Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
copy .env.example .env
```

3. Получите токен бота:
   - Перейдите на [Discord Developer Portal](https://discord.com/developers/applications)
   - Создайте новое приложение
   - Перейдите в раздел "Bot"
   - Скопируйте токен и вставьте его в файл `.env`

4. Пригласите бота на сервер:
   - В разделе "OAuth2" > "URL Generator"
   - Выберите scope: `bot` и `applications.commands`
   - Выберите необходимые права (permissions)
   - Скопируйте сгенерированный URL и откройте его в браузере

## 📝 Использование

### Запуск бота:
```bash
npm start
```

### Запуск в режиме разработки (с автоперезагрузкой):
```bash
npm run dev
```

## 📁 Структура проекта

```
teamnorth-bot/
├── commands/          # Команды бота
│   └── ping.js       # Пример команды
├── index.js          # Главный файл бота
├── package.json      # Зависимости проекта
├── .env              # Переменные окружения (не коммитится)
└── README.md         # Документация
```

## 🛠️ Добавление команд

1. Создайте новый файл в папке `commands/`
2. Используйте следующий шаблон:

```javascript
import { SlashCommandBuilder } from 'discord.js';

export default {
  data: new SlashCommandBuilder()
    .setName('название-команды')
    .setDescription('Описание команды'),
  
  async execute(interaction) {
    await interaction.reply('Ответ бота!');
  },
};
```

3. Перезапустите бота - команда автоматически загрузится!

## 📚 Полезные ссылки

- [Discord.js документация](https://discord.js.org/#/docs)
- [Discord.js Guide](https://discordjs.guide/)
- [Discord Developer Portal](https://discord.com/developers/applications)

